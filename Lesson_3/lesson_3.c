#include <stdio.h>
 
int main(void) {
	char single_symbol = 'a';
	char string_array[] = "string array";
	char *string_pointer = "example string for char pointer"
	
	//print the contnet in the variable
	printf("single_symbol = %c \n", single_symbol);
	printf("string_array = %s \n", string_array);
	printf("string_pointer = %s \n", string_array);
	
	return 0;
}